@model ToursListViewModel
    <h1>@Model.CurrentCategory</h1>
    @foreach(var tour in Model.Tours)
    {
        <div>
            <h2>@tour.Name</h2>
            <h3>@tour.Price.ToString("c")</h3>
            <h4>@tour.Category.CategoryName</h4>
        </div>
    }
